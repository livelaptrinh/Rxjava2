<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="team.kiti.rxjava2_android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".FlowableExampleActivity" />
        <activity android:name=".SingleObserverExampleActivity" />
        <activity android:name=".operators.map.MapExampleActivity">

        </activity>
        <activity android:name=".operators.map.FlatMapExampleActivity" />
        <activity
            android:name=".operators.map.FilterExampleActivity"
            android:label="@string/title_activity_filter_example"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>